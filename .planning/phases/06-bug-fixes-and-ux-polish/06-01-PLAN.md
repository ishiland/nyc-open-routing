---
phase: 06-bug-fixes-and-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/layouts/AdaptiveLayout.tsx
  - client/src/components/controls/MapControls.tsx
autonomous: true

must_haves:
  truths:
    - "User can click the info button in the TitleBar without the collapse toggle intercepting the click"
    - "Map zoom controls appear smoothly on page load without flash or pop-in"
    - "Map zoom controls still function correctly after map loads"
  artifacts:
    - path: "client/src/components/layouts/AdaptiveLayout.tsx"
      provides: "Collapse button repositioned below TitleBar area"
      contains: "top: 48"
    - path: "client/src/components/controls/MapControls.tsx"
      provides: "Disabled placeholder buttons rendered before map loads"
      contains: "disabled"
  key_links:
    - from: "client/src/components/layouts/AdaptiveLayout.tsx"
      to: "client/src/components/shared/TitleBar.tsx"
      via: "Collapse button no longer overlaps TitleBar's info button area"
    - from: "client/src/components/controls/MapControls.tsx"
      to: "client/src/contexts/MapInstanceContext.tsx"
      via: "Renders disabled buttons when map is null, functional when map exists"
---

<objective>
Fix the collapse button overlapping the TitleBar info button (BUG-01) and eliminate map controls flash on page load (MAP-01).

Purpose: The collapse button at top:8,right:8 overlaps with the InfoModal button in TitleBar, making info inaccessible. Map controls returning null before map loads causes a visual pop-in when they appear. Both are interaction/visual bugs users encounter immediately.

Output: AdaptiveLayout.tsx with repositioned collapse button, MapControls.tsx that renders disabled placeholder buttons instead of null.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/components/layouts/AdaptiveLayout.tsx
@client/src/components/controls/MapControls.tsx
@client/src/components/shared/TitleBar.tsx
@client/src/utils/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reposition collapse button below TitleBar</name>
  <files>client/src/components/layouts/AdaptiveLayout.tsx</files>
  <action>
    In AdaptiveLayout.tsx, move the collapse IconButton from `top: 8, right: 8` to `top: 48, right: 8`. The TitleBar's StyledToolbar has minHeight 40px. Setting top:48 places the collapse button 8px below the TitleBar bottom edge, eliminating any overlap with the info button.

    Do NOT change the zIndex value (keep zIndex:1). The positioning change alone resolves the overlap since the button is now in a different vertical area entirely.

    Also do NOT change any other aspect of the collapse button behavior (handleToggle, aria-labels, ref, etc.).
  </action>
  <verify>
    Run `cd /Users/ishiland/Code/nyc-open-routing/client && npx tsc --noEmit` to confirm no type errors.
    Visually: the collapse button should sit below the blue TitleBar, not overlap the info icon.
  </verify>
  <done>Collapse button renders at top:48 instead of top:8, clearing the TitleBar info button area completely.</done>
</task>

<task type="auto">
  <name>Task 2: Render disabled MapControls before map loads</name>
  <files>client/src/components/controls/MapControls.tsx</files>
  <action>
    In MapControls.tsx, replace the early `if (!map) { return null }` guard with rendering the same Fab buttons in a disabled state when map is null. This eliminates the flash/pop-in when the map finishes loading.

    Specific changes:
    1. Remove the `if (!map) { return null }` guard at line 16-18.
    2. Add `const isReady = !!map` variable after the useContext call.
    3. In handleZoomIn and handleZoomOut, the existing `if (!map) return` guards already handle the null case, so no change needed there.
    4. Add `disabled={!isReady}` and `aria-disabled={!isReady}` to each Fab button.
    5. Add `opacity: isReady ? 1 : 0.5` to each Fab's sx to visually indicate disabled state.
    6. Wrap the Tooltip around each Fab only when isReady=true. When disabled, the Fab should not have a tooltip wrapper (Tooltip on disabled elements can cause issues). Use a conditional: `isReady ? <Tooltip ...><Fab .../></Tooltip> : <Fab ... disabled />`. Or simpler: keep the existing structure but add a `componentsProps={{ tooltip: { sx: { display: isReady ? 'block' : 'none' } } }}` — actually the simplest approach is just to add `disabled` to the Fabs and keep Tooltips. MUI Tooltips work on disabled elements when wrapped in a span, but Fab already handles this.

    Simplest approach: Just remove the null guard, add disabled prop to both Fabs when !map, and add reduced opacity. Keep all other styling identical.
  </action>
  <verify>
    Run `cd /Users/ishiland/Code/nyc-open-routing/client && npx tsc --noEmit` to confirm no type errors.
    Run `cd /Users/ishiland/Code/nyc-open-routing/client && npm test` to confirm existing tests pass.
  </verify>
  <done>MapControls renders disabled zoom buttons immediately on page load. When map loads, buttons become enabled with no flash or layout shift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd /Users/ishiland/Code/nyc-open-routing/client && npx tsc --noEmit` passes with no errors
- [ ] `cd /Users/ishiland/Code/nyc-open-routing/client && npm test` passes
- [ ] `cd /Users/ishiland/Code/nyc-open-routing/client && npm run lint:check` passes (or only pre-existing warnings)
- [ ] Collapse button top value is 48, not 8
- [ ] MapControls no longer returns null when map is absent
</verification>

<success_criteria>
- All tasks completed
- No TypeScript compilation errors
- Existing tests pass
- Collapse button positioned below TitleBar (top:48)
- Map controls render immediately with disabled state, then enable when map loads
</success_criteria>

<output>
After completion, create `.planning/phases/06-bug-fixes-and-ux-polish/06-01-SUMMARY.md`
</output>
