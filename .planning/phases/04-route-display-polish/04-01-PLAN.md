---
phase: 04-route-display-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/controls/RouteSummaryCard.tsx
  - client/src/components/controls/RouteList.tsx
  - client/src/components/shared/TurnIcon.tsx
  - client/src/hooks/useMapZoom.ts
  - client/src/hooks/useRouteFetch.ts
  - client/src/components/controls/ButtonControls.tsx
autonomous: true

must_haves:
  truths:
    - "Route summary card border and icon color change dynamically per travel mode (blue for drive, green for bike, orange for walk)"
    - "Turn-by-turn list items show mode-specific icon color and clean typography hierarchy"
    - "Clicking a turn-by-turn step highlights that step with a mode-colored left border and tinted background"
    - "Clicking a turn-by-turn step zooms the map to that segment without over-zooming on short streets"
    - "Switching travel mode or calculating a new route clears any previously selected step highlight"
  artifacts:
    - path: "client/src/components/controls/RouteSummaryCard.tsx"
      provides: "Mode-specific card border, icon color, and traffic chip styling"
      contains: "MODE_COLORS"
    - path: "client/src/components/controls/RouteList.tsx"
      provides: "Active step highlighting, mode-specific turn icon color, visual hierarchy"
      contains: "Mui-selected"
    - path: "client/src/hooks/useMapZoom.ts"
      provides: "maxZoom cap on fitBounds for single-segment zoom"
      contains: "maxZoom"
    - path: "client/src/hooks/useRouteFetch.ts"
      provides: "Clears selectedStreet when new route is calculated"
      contains: "setSelectedStreet"
  key_links:
    - from: "client/src/components/controls/RouteSummaryCard.tsx"
      to: "client/src/utils/theme.ts"
      via: "MODE_COLORS import"
      pattern: "MODE_COLORS\\[mode\\]"
    - from: "client/src/components/controls/RouteList.tsx"
      to: "client/src/contexts/RoutingContext.tsx"
      via: "selectedStreet from context"
      pattern: "selectedStreet.*properties\\.seq"
    - from: "client/src/hooks/useRouteFetch.ts"
      to: "client/src/contexts/RoutingContext.tsx"
      via: "setSelectedStreet(null) on route change"
      pattern: "setSelectedStreet\\(null\\)"
---

<objective>
Polish route display components with mode-specific color accents, improved visual hierarchy, active step highlighting, and refined zoom behavior.

Purpose: Transform the route summary card and turn-by-turn list from functional to polished, applying the MTA transit aesthetic established in Phase 1 with dynamic mode colors and interaction feedback.
Output: Visually polished RouteSummaryCard and RouteList with mode-aware colors, active step visual feedback, and improved zoom behavior.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-route-display-polish/04-RESEARCH.md
@client/src/utils/theme.ts (MODE_COLORS export, theme tokens)
@client/src/contexts/RoutingContext.tsx (selectedStreet, mode, setSelectedStreet)
</context>

<tasks>

<task type="auto">
  <name>Task 1: RouteSummaryCard mode-specific colors and visual polish</name>
  <files>client/src/components/controls/RouteSummaryCard.tsx</files>
  <action>
Import `MODE_COLORS` from `../../utils/theme` and use `MODE_COLORS[mode]` for dynamic color accents throughout the card.

Specific changes:
1. Add import: `import { MODE_COLORS } from "../../utils/theme"`
2. Card `borderColor`: Change from `"primary.main"` to `MODE_COLORS[mode]` — the card's left border should reflect the active travel mode color
3. Mode icon Box `color`: Change from `"primary.main"` to `MODE_COLORS[mode]` — the large mode icon should match the border color
4. Traffic Chip: Change from `color="primary" variant="outlined"` to use mode-specific styling via `sx`: remove the `color="primary"` prop, add `sx={{ ml: "auto", borderColor: MODE_COLORS[mode], color: MODE_COLORS[mode] }}` and set `variant="outlined"`. This makes the traffic chip accent match the mode color instead of always being MTA Blue.
5. Reduce bottom margin on the card from `mb: 2` to `mb: 1.5` for tighter spacing consistent with the compact transit aesthetic.

Do NOT change the grid layout, typography variants, or arrival time section — those already have good visual hierarchy.
  </action>
  <verify>
Run `cd /Users/ishiland/Code/nyc-open-routing/client && npx tsc --noEmit` to verify TypeScript compiles without errors. Grep the file for any remaining `"primary.main"` or `color="primary"` that should have been replaced.
  </verify>
  <done>
RouteSummaryCard uses MODE_COLORS[mode] for borderColor, icon color, and traffic chip accent. No hardcoded "primary.main" remains for mode-dependent elements.
  </done>
</task>

<task type="auto">
  <name>Task 2: RouteList turn-by-turn polish with active step highlighting and mode colors</name>
  <files>
    client/src/components/controls/RouteList.tsx
    client/src/components/shared/TurnIcon.tsx
  </files>
  <action>
**RouteList.tsx changes:**

1. Add imports: `import { MODE_COLORS } from "../../utils/theme"` and pull `mode` and `selectedStreet` from RoutingContext (currently only `route` and `setSelectedStreet` are destructured).

2. Active step highlighting: For each `ListItemButton`, determine if the current step is selected by comparing `selectedStreet?.properties.seq === street.properties.seq`. Pass `selected={isActive}` to `ListItemButton` and add `sx` to override the MUI selected state with mode-specific colors:
```tsx
const isActive = selectedStreet?.properties.seq === street.properties.seq

<ListItemButton
  onClick={() => handleStreetSelect(street)}
  selected={isActive}
  sx={{
    "&.Mui-selected": {
      bgcolor: `${MODE_COLORS[mode]}14`,
      borderLeft: 3,
      borderColor: MODE_COLORS[mode],
      "&:hover": {
        bgcolor: `${MODE_COLORS[mode]}1F`,
      },
    },
  }}
>
```
The `14` suffix is 8% opacity hex alpha, `1F` is 12% for hover — both provide subtle tinting.

3. Turn icon mode color: Remove `color="primary"` from `TurnIcon` and instead pass `sx={{ color: MODE_COLORS[mode] }}`. This requires TurnIcon to accept and forward `sx` — it already does via `...props` spread on `SvgIconProps`.

4. ListItemIcon: Reduce `minWidth` from `40` to `36` for tighter step layout.

5. Typography hierarchy: Add a secondary line showing the street name when `turn_instruction` is present (currently `primary` shows instruction OR street name). Change to:
   - `primary`: `street.properties.turn_instruction || street.properties.street` (keep as-is)
   - `secondary`: Show distance with `formatDistance` (keep as-is)
   - Ensure `slotProps.primary` has `sx: { fontWeight: 500, fontSize: "0.875rem" }` and `slotProps.secondary` has `sx: { fontSize: "0.75rem" }` for clear visual hierarchy.

**TurnIcon.tsx changes:**

The `TurnIcon` component's `color` prop type is restricted to MUI color names (`"primary" | "secondary" | ...`). Since we're now passing color via `sx={{ color: MODE_COLORS[mode] }}` instead of the `color` prop, make the `color` prop optional by updating the default from `"action"` to `"inherit"`. This way the `sx` color takes precedence. The component already spreads `...props` which includes `sx`, so no structural change is needed — just change the default value of `color` from `"action"` to `"inherit"` so the inherited sx color is used when no explicit color prop is passed.
  </action>
  <verify>
Run `cd /Users/ishiland/Code/nyc-open-routing/client && npx tsc --noEmit` to verify TypeScript compiles. Run `cd /Users/ishiland/Code/nyc-open-routing/client && npm test -- --run` to verify existing tests pass.
  </verify>
  <done>
RouteList shows mode-specific TurnIcon colors, active step has a tinted background with mode-colored left border on click, and typography sizes create clear hierarchy between instruction text and distance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Zoom maxZoom cap and clear selectedStreet on route change</name>
  <files>
    client/src/hooks/useMapZoom.ts
    client/src/hooks/useRouteFetch.ts
    client/src/components/controls/ButtonControls.tsx
  </files>
  <action>
**useMapZoom.ts changes:**

Add `maxZoom` option to `UseMapZoomOptions` interface and pass it to `fitBounds`. This prevents over-zooming on short street segments when clicking turn-by-turn steps.

1. Add `maxZoom?: number` to `UseMapZoomOptions` interface with default `17`.
2. Destructure `maxZoom = 17` alongside `padding` in the options.
3. Change `map.fitBounds(bounds, { padding })` to `map.fitBounds(bounds, { padding, maxZoom })`.
4. Add `maxZoom` to the `useCallback` dependency array.

This caps zoom at level 17 (~1-2 blocks of context) preventing the map from zooming to level 20+ on 50-foot segments.

**useRouteFetch.ts changes:**

Clear `selectedStreet` when a new route is successfully calculated. This fixes the bug where a previously selected step persists visually after route recalculation.

1. Add `setSelectedStreet` to the `UseRouteFetchArgs` interface: `setSelectedStreet: (street: RouteFeature | null) => void`. Import `RouteFeature` from `../types/interfaces` (already imported as `Route` is there).
2. In the `fetchRouteCallback`, after `setRoute(data)` on line 115 (the success path where features exist), add `setSelectedStreet(null)` immediately after.
3. Add `setSelectedStreet` to the `useCallback` dependency array.

Then update `ButtonControls.tsx` to pass `setSelectedStreet` to `useRouteFetch`:
4. In `ButtonControls.tsx`, add `setSelectedStreet` to the destructured context values.
5. Pass `setSelectedStreet` in the `useRouteFetch` call options object.
  </action>
  <verify>
Run `cd /Users/ishiland/Code/nyc-open-routing/client && npx tsc --noEmit` to verify TypeScript compiles. Run `cd /Users/ishiland/Code/nyc-open-routing/client && npm test -- --run` to verify all tests pass. Grep `useRouteFetch.ts` for `setSelectedStreet` to confirm it's wired in.
  </verify>
  <done>
fitBounds uses maxZoom: 17 to prevent over-zoom on short segments. New route calculation clears any previously selected turn-by-turn step. ButtonControls passes setSelectedStreet through to useRouteFetch.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/ishiland/Code/nyc-open-routing/client && npx tsc --noEmit` — TypeScript compiles clean
2. `cd /Users/ishiland/Code/nyc-open-routing/client && npm test -- --run` — all existing tests pass
3. `cd /Users/ishiland/Code/nyc-open-routing/client && npm run build` — production build succeeds
4. `cd /Users/ishiland/Code/nyc-open-routing/client && npm run lint:check` — no lint errors
</verification>

<success_criteria>
- RouteSummaryCard border and icon dynamically reflect MODE_COLORS[mode] (blue/green/orange)
- Traffic chip accent matches current mode color
- Turn-by-turn icons use mode-specific colors instead of hardcoded primary blue
- Clicking a step highlights it with mode-colored left border and tinted background
- Map zoom capped at level 17 when clicking turn-by-turn steps
- Selected step clears automatically when a new route is calculated
- All TypeScript compiles, tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-route-display-polish/04-01-SUMMARY.md`
</output>
