# Plan 23-01: Remove Stale Files and Fix References

## Objective

Remove duplicate Dockerfiles, stale artifacts, and fix outdated references so the repo contains only actively-used files.

## Tasks

### Task 1: Delete docs/traffic-audit-report.md (CLN-01)
- `git rm docs/traffic-audit-report.md`
- Internal dev artifact from Phase 19 audit, not useful to contributors

### Task 2: Delete client/Dockerfile (CLN-02)
- `git rm client/Dockerfile`
- Byte-for-byte identical to `docker/dev/client.Dockerfile`
- Unused by docker-compose.yml (which references `docker/dev/client.Dockerfile`)

### Task 3: Delete api/Dockerfile (CLN-03)
- `git rm api/Dockerfile`
- Near-identical to `docker/dev/api.Dockerfile` (only COPY strategy differs)
- Unused by docker-compose.yml (which references `docker/dev/api.Dockerfile`)

### Task 4: Fix stale docker-compose.yml comment (CLN-04)
- Lines 13-17 reference `docs/PERFORMANCE_OPTIMIZATION_PLAN.md` which doesn't exist
- Replace the comment block (lines 13-17) with a simpler version that keeps the tuning context but removes the dead file reference:
```
      # PostgreSQL tuning for development (8-16GB RAM laptops).
      # Production would increase these values significantly:
```

## Verification
1. `docs/traffic-audit-report.md` does not exist
2. `client/Dockerfile` does not exist
3. `api/Dockerfile` does not exist
4. `docker-compose.yml` contains no references to non-existent files
5. `docker compose config` validates successfully (compose file still valid)
