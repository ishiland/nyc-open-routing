# Plan 24-02: Write Architecture Documentation

## Objective

Create a concise `docs/ARCHITECTURE.md` (1-2 pages) covering data flow, import pipeline, routing algorithm, and key SQL functions so a new contributor can understand the system quickly.

## Tasks

### Task 1: Create docs/ARCHITECTURE.md

Sections to cover (DOC-01 requirement):
1. **System Overview** — 3-service Docker Compose setup (client, api, db), brief description of each
2. **Data Flow** — How a route request flows: browser → API → SQL function → pgRouting → GeoJSON response → MapLibre render
3. **Import Pipeline** — `import-lion.sh` → `create_network.py` → 9 SQL phases (01-09), what each does
4. **Routing Algorithm** — pgr_trsp (turn-restricted shortest path), mode-specific costs, traffic factor adjustment
5. **Key SQL Functions** — Brief description of `getdrivingroute()`, `getbikingroute()`, `getwalkingroute()`, isochrone functions, node-snapping functions
6. **Database Schema** — Core tables (edges, edges_vertices_pgr, restrictions, traffic_volumes), key fields on edges

Target: 1-2 pages, concise, not exhaustive. Use existing CLAUDE.md and codebase docs as source material.

## Verification
1. `docs/ARCHITECTURE.md` exists
2. Covers all 4 required topics: data flow, import pipeline, routing algorithm, key SQL functions
3. Is concise (under ~150 lines)
