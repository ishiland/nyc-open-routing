---
phase: 14-code-quality-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/test_bike_debug.py
  - api/dependencies.py
  - api/exceptions.py
  - api/main.py
  - api/config/settings.py
  - api/middleware/logging.py
  - api/models/schemas.py
  - api/routes/health.py
  - api/routes/isochrone.py
  - api/routes/routing.py
  - api/routes/search.py
  - api/services/isochrone.py
  - api/services/routing.py
  - api/services/search.py
  - api/utils/cache.py
  - api/utils/clock.py
  - api/utils/geo.py
  - api/utils/retry.py
  - api/conftest.py
  - api/tests/test_cache.py
  - api/tests/test_health.py
  - api/tests/test_routes.py
  - api/tests/test_services.py
  - api/tests/test_smoke.py
  - api/tests/test_utils.py
  - data-importer/src/sql/05_functions.sql
  - data-importer/src/sql/analysis_queries.sql
  - data-importer/src/sql/test_turn_functions.sql
  - data-importer/src/create_network.py
  - data-importer/src/traffic_volumes.py
  - data-importer/src/utils.py
  - data-importer/import-lion.sh
  - scripts/diagnose_topology.py
  - scripts/import_traffic.py
  - scripts/import_traffic_speeds.py
  - scripts/performance-test.sh
  - docker-compose.yml
autonomous: true

must_haves:
  truths:
    - "No dead code in backend -- unused imports, debug files, commented-out blocks, and orphaned functions are removed"
    - "Variable and function names in Python and SQL accurately describe their purpose"
    - "Control flow is straightforward -- no tangled state, redundant branches, or logic that requires mental gymnastics"
    - "All existing backend tests pass with zero regressions"
  artifacts:
    - path: "api/"
      provides: "Clean Python backend with no dead code or misleading names"
    - path: "data-importer/src/sql/05_functions.sql"
      provides: "Clean SQL functions with no dead code or misleading names"
    - path: "scripts/"
      provides: "Clean utility scripts with no dead code"
  key_links:
    - from: "api/services/routing.py"
      to: "api/dependencies.py"
      via: "dependency injection"
      pattern: "get_routing_service"
    - from: "api/routes/routing.py"
      to: "api/services/routing.py"
      via: "Depends() injection"
      pattern: "Depends.*routing_service"
---

<objective>
Review the entire backend codebase (api/, data-importer/, scripts/) for KISS violations and fix all issues found.

Purpose: Ensure every backend module is simple, correct, and easy to follow before public release.
Output: Clean backend code with zero dead code, accurate names, and straightforward control flow.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and clean backend Python code (api/)</name>
  <files>
    api/test_bike_debug.py
    api/dependencies.py
    api/exceptions.py
    api/main.py
    api/config/settings.py
    api/middleware/logging.py
    api/models/schemas.py
    api/routes/health.py
    api/routes/isochrone.py
    api/routes/routing.py
    api/routes/search.py
    api/services/isochrone.py
    api/services/routing.py
    api/services/search.py
    api/utils/cache.py
    api/utils/clock.py
    api/utils/geo.py
    api/utils/retry.py
    api/conftest.py
    api/tests/test_cache.py
    api/tests/test_health.py
    api/tests/test_routes.py
    api/tests/test_services.py
    api/tests/test_smoke.py
    api/tests/test_utils.py
  </files>
  <action>
    Read every Python file in api/ (source + tests). For each file, check for and fix:

    **Dead code removal:**
    - Delete `api/test_bike_debug.py` — this is a debug test file at the api root, not in the test directory, clearly leftover from development
    - Remove unused imports (check each import is actually used in the file)
    - Remove commented-out code blocks (not explanatory comments — actual dead code behind `#`)
    - Remove unreachable branches (code after unconditional return/raise)
    - Remove orphaned functions/methods that nothing calls (grep the codebase to verify)

    **Misleading names:**
    - Check that function names describe what they do (not what they used to do)
    - Check that variable names are self-documenting (no single-letter names except `i/j/k` in loops, `e` in exceptions)
    - Check that module names match their contents
    - Fix any name that would confuse a new reader

    **Unnecessary abstractions:**
    - Look for wrapper functions that just call through to another function with no added value
    - Look for classes that could be simple functions
    - Look for over-parameterized functions where some params are never varied
    - Simplify without changing behavior

    **Control flow:**
    - Flatten deeply nested if/else chains where possible (early returns, guard clauses)
    - Remove redundant boolean checks (`if x == True:` -> `if x:`)
    - Simplify try/except blocks that catch too broadly or re-raise without adding value

    **Important constraints:**
    - Do NOT change any public API signatures (function params, return types, HTTP endpoints)
    - Do NOT change any SQL function calls (the SQL function names and params are a contract)
    - Do NOT modify test assertions — only clean up test code structure/imports
    - Preserve all docstrings and explanatory comments
    - Run `cd /Users/ishiland/Code/nyc-open-routing && docker compose exec api pytest` after changes to verify tests still pass (if containers are running), OR note that test verification is needed
  </action>
  <verify>
    1. `api/test_bike_debug.py` no longer exists
    2. `grep -rn "^import\|^from" api/ --include="*.py"` — every import is used in its file
    3. No commented-out code blocks remain (brief inline comments explaining WHY are fine)
    4. All test files still have the same test count (no tests deleted)
  </verify>
  <done>
    Every Python file in api/ is clean: no unused imports, no dead code, no misleading names, no unnecessary abstractions. The debug file is deleted. All existing tests pass unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and clean data-importer, scripts, and config files</name>
  <files>
    data-importer/src/sql/05_functions.sql
    data-importer/src/sql/analysis_queries.sql
    data-importer/src/sql/test_turn_functions.sql
    data-importer/src/create_network.py
    data-importer/src/traffic_volumes.py
    data-importer/src/utils.py
    data-importer/import-lion.sh
    scripts/diagnose_topology.py
    scripts/import_traffic.py
    scripts/import_traffic_speeds.py
    scripts/performance-test.sh
    docker-compose.yml
  </files>
  <action>
    Read every file in data-importer/ and scripts/. For each file, check for and fix:

    **Dead code in SQL (05_functions.sql is the main target — it contains all routing functions):**
    - Look for commented-out SQL blocks that are dead code (not documentation)
    - Look for unused variables or CTEs within functions
    - Look for duplicate logic that could be consolidated (but do NOT change function signatures — they are called by the Python service layer)
    - Check that function/column names are clear and accurate

    **Dead code in analysis_queries.sql and test_turn_functions.sql:**
    - These are development/debug SQL files. Evaluate if they still serve a purpose. If they contain only one-off analysis queries that were used during development and have no ongoing value, note them but do NOT delete (they may be useful reference for future debugging)

    **Dead code in Python (create_network.py, traffic_volumes.py, utils.py):**
    - Same checks as Task 1: unused imports, commented-out blocks, misleading names, unnecessary abstractions
    - Check utils.py for functions that are no longer called by create_network.py or traffic_volumes.py

    **Shell scripts (import-lion.sh, performance-test.sh):**
    - Check for dead code paths, unused variables, commented-out sections
    - Verify script names match their actual purpose

    **Scripts directory (scripts/):**
    - Read each script. Check for dead code, misleading names, and whether each script is still relevant
    - `diagnose_topology.py` — development diagnostic tool, keep if useful
    - `import_traffic.py` and `import_traffic_speeds.py` — check for redundancy between these two
    - `performance-test.sh` — check if still functional

    **docker-compose.yml:**
    - Check for commented-out services, unused environment variables, dead volume mounts

    **Important constraints:**
    - Do NOT rename SQL functions — they are called by name from Python service layer
    - Do NOT change SQL function parameter signatures
    - Do NOT delete analysis_queries.sql or test_turn_functions.sql without explicit justification
    - Preserve all comments that explain WHY something is done a certain way (especially in 05_functions.sql where pgRouting quirks are documented)
  </action>
  <verify>
    1. No commented-out dead code blocks remain in SQL functions (explanatory comments preserved)
    2. No unused imports in data-importer Python files
    3. Shell scripts have no dead code paths
    4. docker-compose.yml has no commented-out dead sections
  </verify>
  <done>
    All data-importer files, scripts, and docker-compose.yml are clean: no dead code, accurate names, straightforward logic. SQL function signatures are unchanged. Backend tests still pass.
  </done>
</task>

</tasks>

<verification>
1. `api/test_bike_debug.py` does not exist
2. All Python files have no unused imports: `docker compose exec api make lint` passes (or manual grep verification)
3. No commented-out code blocks in any Python or SQL file (legitimate comments preserved)
4. All existing tests pass: `docker compose exec api pytest` returns 0 exit code
5. No SQL function signatures changed (grep for function names in routing.py and isochrone.py matches SQL function definitions)
</verification>

<success_criteria>
- Zero dead code in backend: no unused imports, no debug files, no commented-out blocks, no orphaned functions
- All names are accurate and self-documenting
- Control flow is clean and easy to trace
- All existing tests pass with zero regressions
- SQL function signatures unchanged (Python-SQL contract preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/14-code-quality-review/14-01-SUMMARY.md`
</output>
