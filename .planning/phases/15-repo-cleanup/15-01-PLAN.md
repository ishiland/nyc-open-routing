---
phase: 15-repo-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .github/workflows/ci.yml
  - .env
autonomous: true

must_haves:
  truths:
    - "No test/debug screenshots exist in tracked files or working tree root"
    - "client/dist/ is in .gitignore and fully untracked from git"
    - "Internal dev artifacts (.planning/, CLAUDE.md, .playwright-mcp/) are removed from git tracking and docs/ directory is deleted from disk"
    - ".gitignore covers comprehensive patterns for OS files, editor configs, build outputs, env files, and debug artifacts"
    - "GitHub Actions workflow references correct branch (master), current action versions, and correct Node version"
  artifacts:
    - path: ".gitignore"
      provides: "Comprehensive ignore patterns for public open-source repo"
      contains: "client/dist"
    - path: ".github/workflows/ci.yml"
      provides: "Updated CI workflow with current action versions and correct branch"
      contains: "master"
    - path: ".env.example"
      provides: "Template env file with default dev credentials (replaces tracked .env)"
      contains: "POSTGRES_PASSWORD"
  key_links:
    - from: ".gitignore"
      to: "client/dist/"
      via: "gitignore pattern prevents dist from being tracked"
      pattern: "client/dist"
    - from: ".github/workflows/ci.yml"
      to: "master branch"
      via: "branch trigger references correct default branch"
      pattern: "branches:.*master"
---

<objective>
Clean the repository for public release by removing all test screenshots, internal dev artifacts, and junk files from git tracking, updating .gitignore with comprehensive patterns, converting the tracked .env to .env.example, and updating the CI workflow to current action versions with the correct branch name.

Purpose: Repository should contain only files that belong in a public open-source project.
Output: Clean git state with updated .gitignore, .env.example, and CI workflow.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.gitignore
@.github/workflows/ci.yml
@.env
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore with comprehensive patterns and convert .env to .env.example</name>
  <files>.gitignore, .env, .env.example</files>
  <action>
**Step 1: Rewrite .gitignore** with comprehensive patterns. The current .gitignore is minimal (only .idea/, client/node_modules/, client/build/, *.log, __pycache__, .DS_Store, /api/gde, *.csv). Replace entirely with:

```
# OS files
.DS_Store
Thumbs.db
Desktop.ini
._*

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
*.egg
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Node / Client
client/node_modules/
client/dist/
client/build/
*.log

# Environment files
.env
docker/dev/.env

# API
/api/gde

# Data files
*.csv

# Debug / Test artifacts
*.png
*.jpeg
*.jpg
.playwright-mcp/

# GSD planning (internal dev)
.planning/

# Claude Code
CLAUDE.md

# Internal dev docs
docs/

# Misc junk
todo.md
lion meta.md
ROUTING_ANALYSIS.md
```

Note: The `*.png` pattern in .gitignore is safe because the only legitimate PNG (favicon) lives at `client/public/favicon.png` and `client/dist/favicon.png` — dist is already ignored, and we need to explicitly un-ignore client/public/favicon.png. Add this AFTER the `*.png` line:
```
# Exception: keep favicon in public/
!client/public/favicon.png
```

**Step 2: Convert .env to .env.example.** The root `.env` file is tracked in git and has default dev credentials. Since `docker-compose.yml` references `docker/dev/.env` (not the root `.env`), the root `.env` is redundant but useful as a template for documentation purposes.

1. Copy `.env` to `.env.example` (same content — it only has default dev credentials, not real secrets)
2. The tracked `.env` will be removed from git in Task 2 (covered by the new .gitignore pattern)

The `.env.example` should have the same content as the current `.env`:
```
DEFAULT_LION=25a
DEFAULT_GEOSUPPORT=25a

# database
POSTGRES_DB=routing
POSTGRES_USER=postgres
POSTGRES_PASSWORD=admin
POSTGRES_PORT=5432
POSTGRES_HOST=db

# api
FASTAPI_PORT=3000

# client
NODE_ENV=development
CHOKIDAR_USEPOLLING=true
```

  </action>
  <verify>
1. `cat .gitignore` shows comprehensive patterns including `client/dist/`, `.planning/`, `CLAUDE.md`, `*.png`, `.env`, `docs/`, `!client/public/favicon.png`
2. `.env.example` exists with the default dev credentials
3. `cat .gitignore | grep -c '^[^#]'` shows 25+ non-comment lines (was 8)
  </verify>
  <done>.gitignore covers all required patterns (OS, editor, Python, Node, build outputs, env, debug artifacts, internal dev files). .env.example exists as a template.</done>
</task>

<task type="auto">
  <name>Task 2: Remove tracked files and untrack directories that should be ignored</name>
  <files>(git index changes + disk cleanup of untracked files)</files>
  <action>
**IMPORTANT:** Use `git rm` for files that should be deleted from disk AND git. Use `git rm --cached` for files/dirs that should stop being tracked but remain on disk.

**Step 1: Remove tracked screenshots from git AND disk.**
These are test/debug screenshots tracked in git:
```bash
git rm screen.png
```
(Only `screen.png` is tracked — the other root PNGs/JPEGs are untracked per `git status`)

Also delete the untracked screenshots from disk (not git rm, just plain rm since they are not tracked):
```bash
rm -f fdr-williamsburg-route.png ferry-fix-bike-route.png ferry-fix-full-route.png ferry-fix-isochrone.png ferry-fix-zoomed.png ferry-route-bug.png layer-order-test.png test-01-default-state.png test-02-expanded-picker.png test-03-summary-state.png test-07-deep-link.png test1-two-point-route.jpeg test2-waypoint-route.jpeg test3-waypoint-removed.jpeg
```

**Step 2: Remove tracked junk files from git AND disk.**
```bash
git rm "lion meta.md"
git rm ROUTING_ANALYSIS.md
git rm .env
```
(`todo.md` is untracked, just delete from disk: `rm -f todo.md`)

**Step 3: Remove internal dev artifacts from git tracking (keep on disk).**
```bash
git rm --cached CLAUDE.md
git rm --cached -r .planning/
```

**Step 4: Untrack client/dist/ (keep on disk for local dev).**
```bash
git rm --cached -r client/dist/
```

**Step 5: Delete docs/ directory from disk.**
`docs/` contains 14 internal dev notes (IMPLEMENTATION_COMPLETE.md, MOBILE_RESPONSIVE_IMPLEMENTATION.md, PERFORMANCE_BASELINE.md, etc.) that are not public documentation. The directory is untracked (not in git index), so use plain `rm`, not `git rm`:
```bash
rm -rf docs/
```

**Step 6: Untrack docker/dev/.env if tracked.**
Check if `docker/dev/.env` is currently tracked:
```bash
git ls-files --cached docker/dev/.env
```
If output is non-empty, untrack it (keep on disk):
```bash
git rm --cached docker/dev/.env
```

**Step 7: Verify no screenshots remain in tracked files.**
```bash
git ls-files --cached '*.png' '*.jpeg' '*.jpg'
```
Expected output: only `client/public/favicon.png` (if tracked). No root-level images.

**Step 8: Verify client/dist is fully untracked.**
```bash
git ls-files --cached 'client/dist/'
```
Expected: empty output.
  </action>
  <verify>
1. `git ls-files --cached '*.png' '*.jpeg' '*.jpg'` shows only `client/public/favicon.png` (no root screenshots, no dist PNGs)
2. `git ls-files --cached 'client/dist/'` returns empty
3. `git ls-files --cached '.planning/'` returns empty
4. `git ls-files --cached 'CLAUDE.md'` returns empty
5. `git ls-files --cached '.env'` returns empty
6. `git ls-files --cached 'lion meta.md' 'ROUTING_ANALYSIS.md'` returns empty
7. `ls *.png *.jpeg 2>/dev/null` returns empty (screenshots deleted from disk)
8. `ls todo.md 2>/dev/null` returns empty
9. `test ! -d docs/` succeeds (docs/ directory deleted from disk)
10. `test -f .planning/STATE.md` succeeds (still on disk, just untracked)
  </verify>
  <done>All test/debug screenshots removed. client/dist/ untracked. .planning/, CLAUDE.md removed from git tracking (still on disk). docs/ directory deleted from disk. Junk files (todo.md, lion meta.md, ROUTING_ANALYSIS.md, .env) removed.</done>
</task>

<task type="auto">
  <name>Task 3: Update GitHub Actions CI workflow to current versions and correct branch</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Update `.github/workflows/ci.yml` with the following changes:

**1. Fix branch references** — repo default branch is `master`, not `main`:
```yaml
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
```

**2. Update action versions** to current (as of Feb 2026):
- `actions/checkout@v3` -> `actions/checkout@v4` (v4 is the safe stable choice; v6 exists but v4 is widely used)
- `actions/setup-node@v3` -> `actions/setup-node@v4`
- `actions/setup-python@v4` -> `actions/setup-python@v5`
- `docker/setup-buildx-action@v2` -> `docker/setup-buildx-action@v3`
- `docker/build-push-action@v4` -> `docker/build-push-action@v6`
- `codecov/codecov-action@v3` -> `codecov/codecov-action@v5`

**3. Update Node version** — project uses Node 23 (per client/Dockerfile `FROM node:23.10.0-alpine`):
```yaml
node-version: 23
```

**4. No other changes.** Keep the same job structure, step names, and overall workflow logic.
  </action>
  <verify>
1. `grep 'branches:' .github/workflows/ci.yml` shows `master` (not `main`)
2. `grep 'checkout@' .github/workflows/ci.yml` shows `v4`
3. `grep 'setup-node@' .github/workflows/ci.yml` shows `v4`
4. `grep 'setup-python@' .github/workflows/ci.yml` shows `v5`
5. `grep 'setup-buildx-action@' .github/workflows/ci.yml` shows `v3`
6. `grep 'build-push-action@' .github/workflows/ci.yml` shows `v6`
7. `grep 'codecov-action@' .github/workflows/ci.yml` shows `v5`
8. `grep 'node-version:' .github/workflows/ci.yml` shows `23`
  </verify>
  <done>CI workflow uses correct branch name (master), current action versions, and correct Node version (23).</done>
</task>

</tasks>

<verification>
After all tasks complete, run these overall checks:

1. **No screenshots in tracked files:**
   `git ls-files --cached '*.png' '*.jpeg' '*.jpg'` — only `client/public/favicon.png`

2. **No screenshots on disk at root:**
   `ls *.png *.jpeg *.jpg 2>/dev/null` — empty

3. **client/dist/ fully untracked:**
   `git ls-files --cached 'client/dist/'` — empty

4. **Internal artifacts untracked:**
   `git ls-files --cached '.planning/' 'CLAUDE.md'` — empty

5. **docs/ directory removed from disk:**
   `test ! -d docs/ && echo OK`

6. **Junk files gone:**
   `git ls-files --cached 'todo.md' 'lion meta.md' 'ROUTING_ANALYSIS.md' '.env'` — empty

7. **.gitignore is comprehensive:**
   `wc -l .gitignore` — 40+ lines

8. **.env.example exists:**
   `test -f .env.example && echo OK`

9. **CI workflow correct:**
   `grep -c 'master' .github/workflows/ci.yml` — 2 (push + PR branches)
   `grep 'node-version:' .github/workflows/ci.yml` — shows 23

10. **On-disk files preserved:**
   `test -d .planning && test -f CLAUDE.md && echo "local files intact"`
</verification>

<success_criteria>
- Zero test/debug screenshots in tracked files or repo root
- client/dist/ is gitignored and untracked
- .planning/, CLAUDE.md removed from git tracking; docs/ deleted from disk
- .gitignore has comprehensive patterns covering all categories
- .env.example exists with default dev credentials
- CI workflow targets master branch with current action versions and Node 23
</success_criteria>

<output>
After completion, create `.planning/phases/15-repo-cleanup/15-01-SUMMARY.md`
</output>
