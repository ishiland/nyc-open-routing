---
phase: 03-sidebar-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/controls/TravelModeSelect.tsx
  - client/src/components/controls/TrafficToggle.tsx
  - client/src/components/controls/FerryToggle.tsx
  - client/src/components/ControlsContainer.tsx
  - client/src/components/shared/TitleBar.tsx
  - client/src/utils/theme.ts
autonomous: true

must_haves:
  truths:
    - "Travel mode selector renders as a compact horizontal ToggleButtonGroup (~44px) instead of stacked AppBar+Tabs (~64px)"
    - "Selected ToggleButton shows mode-specific color from MODE_COLORS (drive=blue, bike=green, walk=orange)"
    - "Traffic toggle (drive mode) and ferry toggle (bike/walk) render as compact inline rows (~28px) below the mode selector"
    - "TitleBar is visually compact (~40px instead of ~48px)"
    - "Clicking already-selected mode button does NOT deselect (mode cannot be null)"
  artifacts:
    - path: "client/src/components/controls/TravelModeSelect.tsx"
      provides: "ToggleButtonGroup-based mode selector with MODE_COLORS"
      contains: "ToggleButtonGroup"
    - path: "client/src/components/controls/TrafficToggle.tsx"
      provides: "Compact inline traffic toggle without borderBottom"
    - path: "client/src/components/controls/FerryToggle.tsx"
      provides: "Compact inline ferry toggle without borderBottom"
    - path: "client/src/components/ControlsContainer.tsx"
      provides: "Restructured sidebar layout with tighter spacing"
    - path: "client/src/utils/theme.ts"
      provides: "MuiToggleButton and MuiToggleButtonGroup theme overrides"
      contains: "MuiToggleButton"
  key_links:
    - from: "client/src/components/controls/TravelModeSelect.tsx"
      to: "client/src/utils/theme.ts"
      via: "MODE_COLORS import for selected button color"
      pattern: "MODE_COLORS"
    - from: "client/src/components/controls/TravelModeSelect.tsx"
      to: "client/src/contexts/RoutingContext.tsx"
      via: "mode and setMode from RoutingContext"
      pattern: "useContext.*RoutingContext"
---

<objective>
Replace the vertically-spread sidebar header (AppBar+Tabs mode selector, full-row toggles, 48px TitleBar) with a compact transit-inspired controls area using ToggleButtonGroup, inline toggle rows, and a tightened TitleBar.

Purpose: Reclaim ~50px of vertical space in the sidebar header while making the mode selector visually stronger with mode-specific colors.
Output: Compact sidebar header with ToggleButtonGroup mode selector, inline toggles, tighter TitleBar, and ToggleButton theme overrides.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sidebar-redesign/03-RESEARCH.md
@.planning/phases/01-design-system-foundation/01-01-SUMMARY.md

Key existing state:
- TravelModeSelect.tsx uses AppBar+Tabs with 64px minHeight tabs, gets mode/setMode from RoutingContext
- TrafficToggle.tsx and FerryToggle.tsx use FormControlLabel+Switch with borderBottom and paddingX:2/paddingY:1
- ControlsContainer.tsx stacks: TitleBar, TravelModeSelect, TrafficToggle, FerryToggle, then Box(padding:2) for children
- TitleBar.tsx uses styled AppBar+Toolbar at 48px minHeight with 16px padding
- theme.ts has MODE_COLORS, 6px spacing base, single-pass createTheme, MuiTab overrides (to be replaced)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace AppBar+Tabs with ToggleButtonGroup in TravelModeSelect and add theme overrides</name>
  <files>
    client/src/components/controls/TravelModeSelect.tsx
    client/src/utils/theme.ts
  </files>
  <action>
**TravelModeSelect.tsx -- full rewrite:**
- Remove imports: AppBar, Tabs, Tab
- Add imports: ToggleButtonGroup, ToggleButton from "@mui/material"
- Import MODE_COLORS from "../../utils/theme"
- Keep existing imports: DirectionsBike, DirectionsCar, DirectionsWalk from "@mui/icons-material", RoutingContext and TravelMode from contexts
- Replace component body with ToggleButtonGroup:
  - `value={mode}`, `exclusive`, `fullWidth`, `size="small"`
  - `onChange={(_, newMode: TravelMode | null) => newMode && setMode(newMode)}` -- the `newMode &&` guard prevents deselection (Pitfall 2 from research)
  - `aria-label="Travel mode"`
- Three ToggleButton children for drive/bike/walk:
  - Each gets `value="drive"|"bike"|"walk"`, `aria-label="Driving|Biking|Walking directions"`
  - Each gets `sx={{ minHeight: 44, gap: 0.5 }}` for touch target and icon-label spacing
  - Icon: `<DirectionsCar|Bike|Walk fontSize="small" />` then text label "Drive"|"Bike"|"Walk"
- Apply mode-specific selected color using sx on ToggleButtonGroup:
  ```
  sx={{
    "& .Mui-selected": {
      bgcolor: MODE_COLORS[mode] + " !important",
      color: "#fff !important",
      "&:hover": {
        bgcolor: MODE_COLORS[mode] + " !important",
        opacity: 0.9,
      },
    },
  }}
  ```
  This makes the selected button's background match the active mode color (blue for drive, green for bike, orange for walk).

**theme.ts -- add ToggleButton overrides:**
- Add MuiToggleButton to `components`:
  ```
  MuiToggleButton: {
    styleOverrides: {
      root: {
        fontWeight: 600,
        textTransform: "none" as const,
        borderRadius: 6,
        minHeight: 44,
        "&:focus-visible": {
          outline: "3px solid",
          outlineColor: MTA_BLUE,
          outlineOffset: "2px",
        },
      },
    },
  },
  MuiToggleButtonGroup: {
    styleOverrides: {
      root: {
        borderRadius: 6,
      },
    },
  },
  ```
- The existing MuiTab overrides can remain (they don't conflict and may be used elsewhere).
  </action>
  <verify>
Run `cd /Users/ishiland/Code/nyc-open-routing/client && npx tsc --noEmit` -- no type errors in TravelModeSelect.tsx or theme.ts.
Visually verify (if possible) that ToggleButtonGroup renders three mode buttons horizontally.
  </verify>
  <done>
TravelModeSelect renders a horizontal ToggleButtonGroup with 3 mode buttons. Selected button shows mode-specific color from MODE_COLORS. Clicking already-selected button does nothing (no deselection). Theme has ToggleButton overrides with 44px touch target and focus-visible ring.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compact toggles, TitleBar, and restructure ControlsContainer layout</name>
  <files>
    client/src/components/controls/TrafficToggle.tsx
    client/src/components/controls/FerryToggle.tsx
    client/src/components/shared/TitleBar.tsx
    client/src/components/ControlsContainer.tsx
  </files>
  <action>
**TrafficToggle.tsx -- compact to inline row:**
- Keep the same mode guard (`if (mode !== "drive") return null`)
- Remove `borderBottom: "1px solid rgba(0, 0, 0, 0.12)"` from the outer Box sx
- Change outer Box sx to: `{ display: "flex", alignItems: "center", gap: 0.5, px: 2, py: 0.5 }`
- Replace FormControlLabel with direct Switch + icon + label in the flex row:
  ```
  <Switch size="small" checked={useTraffic} onChange={e => setUseTraffic(e.target.checked)} color="primary" />
  <TrafficIcon fontSize="small" sx={{ color: "primary.main" }} />
  <Typography variant="caption" fontWeight={500}>Traffic</Typography>
  ```
- Keep the Tooltip wrapper around the entire Box
- Remove FormControlLabel import if no longer used

**FerryToggle.tsx -- compact to inline row (same pattern):**
- Keep the mode guard (`if (mode === "drive") return null`)
- Remove `borderBottom: "1px solid rgba(0, 0, 0, 0.12)"` from the outer Box sx
- Change outer Box sx to: `{ display: "flex", alignItems: "center", gap: 0.5, px: 2, py: 0.5 }`
- Replace FormControlLabel with direct Switch + icon + label in the flex row:
  ```
  <Switch size="small" checked={avoidFerries} onChange={e => setAvoidFerries(e.target.checked)} color="primary" />
  <DirectionsBoatIcon fontSize="small" sx={{ color: "primary.main" }} />
  <Typography variant="caption" fontWeight={500}>Avoid ferries</Typography>
  ```
- Keep the Tooltip wrapper
- Remove FormControlLabel import if no longer used

**TitleBar.tsx -- compact:**
- Change StyledToolbar minHeight from "48px" to "40px"
- Change StyledToolbar paddingLeft/paddingRight from "16px" to "12px"
- Change Title fontSize from "20px" to "16px"
- Keep fontWeight, flexGrow, contrastText color unchanged

**ControlsContainer.tsx -- restructure spacing:**
- Change the children Box padding from `padding: 2` (12px) to `padding: 1.5` (9px) to tighten below the toggles
- Optionally add a `Box` wrapper around `TravelModeSelect + TrafficToggle + FerryToggle` with `sx={{ px: 1.5, pt: 1 }}` to give them slight padding without the full section padding. This visually groups the mode selector and toggle as a unit.
- Keep existing Paper, TitleBar, and component ordering unchanged.
  </action>
  <verify>
Run `cd /Users/ishiland/Code/nyc-open-routing/client && npx tsc --noEmit` -- no type errors.
Confirm that TrafficToggle only shows for drive mode and FerryToggle only for bike/walk (mode conditional logic preserved).
  </verify>
  <done>
TrafficToggle and FerryToggle render as compact inline rows (~28px each) without bottom borders. TitleBar is 40px tall with 16px font. ControlsContainer has tighter padding. Mode-conditional visibility preserved (traffic for drive, ferry for bike/walk).
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd /Users/ishiland/Code/nyc-open-routing/client && npx tsc --noEmit`
2. Client builds: `cd /Users/ishiland/Code/nyc-open-routing/client && npm run build`
3. Existing tests pass: `cd /Users/ishiland/Code/nyc-open-routing/client && npm test -- --run`
4. ToggleButtonGroup replaces Tabs in TravelModeSelect (no AppBar/Tabs imports remain)
5. MODE_COLORS used for selected button color, not hardcoded hex
6. Toggle components use compact inline layout without borderBottom
7. TitleBar reduced from 48px to 40px
</verification>

<success_criteria>
- TravelModeSelect renders ToggleButtonGroup (not Tabs) with mode-specific selected colors
- TrafficToggle and FerryToggle are compact inline rows
- TitleBar is 40px tall
- ControlsContainer has tighter spacing
- All existing functionality preserved (mode switching, toggle visibility by mode)
- TypeScript compiles and client builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-sidebar-redesign/03-01-SUMMARY.md`
</output>
