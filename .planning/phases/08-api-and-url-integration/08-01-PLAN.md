---
phase: 08-api-and-url-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Route API calls include hour and day_of_week params when departure time is set"
    - "Isochrone API calls include hour and day_of_week params when departure time is set"
    - "URL query params reflect selected departure time (hour and day)"
    - "Opening a shared link with hour/day params restores the picker to the correct day and hour"
  artifacts:
    - path: "client/src/hooks/useRouteFetch.ts"
      provides: "Route fetch with hour/day_of_week API params"
      contains: "hour=${trafficHour}&day_of_week=${trafficDayOfWeek}"
    - path: "client/src/hooks/useIsochroneFetch.ts"
      provides: "Isochrone fetch with hour/day_of_week API params"
      contains: "hour=${trafficHour}&day_of_week=${trafficDayOfWeek}"
    - path: "client/src/hooks/useRouteStateSync.ts"
      provides: "URL sync for hour and day params (read and write)"
      contains: "params.set(\"hour\""
    - path: "client/src/contexts/RoutingContext.tsx"
      provides: "trafficHour and trafficDayOfWeek state with localStorage persistence"
      contains: "trafficHour"
  key_links:
    - from: "client/src/contexts/RoutingContext.tsx"
      to: "client/src/hooks/useRouteFetch.ts"
      via: "trafficHour/trafficDayOfWeek passed as args"
      pattern: "trafficHour.*trafficDayOfWeek"
    - from: "client/src/contexts/RoutingContext.tsx"
      to: "client/src/hooks/useIsochroneFetch.ts"
      via: "trafficHour/trafficDayOfWeek passed as args"
      pattern: "trafficHour.*trafficDayOfWeek"
    - from: "client/src/hooks/useRouteStateSync.ts"
      to: "client/src/contexts/RoutingContext.tsx"
      via: "setTrafficHour/setTrafficDayOfWeek on URL restore"
      pattern: "setTrafficHour|setTrafficDayOfWeek"
    - from: "client/src/components/controls/ButtonControls.tsx"
      to: "client/src/hooks/useRouteFetch.ts"
      via: "auto-recalculate on trafficHour/trafficDayOfWeek change"
      pattern: "trafficHour.*trafficDayOfWeek"
---

<objective>
Verify that Phase 8 requirements (TIME-04, TIME-05) are already fully implemented by existing code.

Purpose: Phase 7 planning discovered that the API integration and URL sync wiring was built during v2.0. This plan confirms all four success criteria are met and creates the phase SUMMARY without any code changes.

Output: Verification confirmation and phase SUMMARY documenting the pre-existing implementation.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/hooks/useRouteFetch.ts
@client/src/hooks/useIsochroneFetch.ts
@client/src/hooks/useRouteStateSync.ts
@client/src/contexts/RoutingContext.tsx
@client/src/components/controls/ButtonControls.tsx
@client/src/components/controls/IsochroneControls.tsx
@client/src/components/controls/DepartureTimePicker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all Phase 8 success criteria are met by existing code</name>
  <files></files>
  <action>
Verify each of the four success criteria by reading the source files and confirming the wiring exists. No code changes needed.

**Criterion 1: Route API calls include hour and day_of_week params**
- Read `client/src/hooks/useRouteFetch.ts`
- Confirm lines 80-82 append `&hour=${trafficHour}&day_of_week=${trafficDayOfWeek}` when `useTraffic && trafficHour !== null && trafficDayOfWeek !== null`
- Confirm `trafficHour` and `trafficDayOfWeek` are in the useCallback dependency array (line 154)

**Criterion 2: Isochrone API calls include hour and day_of_week params**
- Read `client/src/hooks/useIsochroneFetch.ts`
- Confirm lines 60-63 append `&hour=${trafficHour}&day_of_week=${trafficDayOfWeek}` with the same guard condition
- Confirm `IsochroneControls.tsx` passes `trafficHour`/`trafficDayOfWeek` from context to the hook

**Criterion 3: Departure time reflected in URL query params**
- Read `client/src/hooks/useRouteStateSync.ts`
- Confirm the URL-write effect (lines 165-168) sets `hour` and `day` params when traffic time is active
- Confirm `trafficHour` and `trafficDayOfWeek` are in the effect dependency array (line 185)

**Criterion 4: Shared link restores picker to correct day/hour**
- Read `client/src/hooks/useRouteStateSync.ts`
- Confirm the URL-read effect (lines 75-86) parses `hour` and `day` from URL params, validates ranges (hour 0-23, day 1-7), and calls `setTrafficHour`/`setTrafficDayOfWeek`
- Confirm `DepartureTimePicker.tsx` reads `trafficHour`/`trafficDayOfWeek` from RoutingContext to render the correct picker state

**Additional verification:**
- Confirm `ButtonControls.tsx` includes `trafficHour` and `trafficDayOfWeek` in its auto-recalculate useEffect dependency array (line 65), so changing departure time triggers a re-fetch
- Run `npm run build` in the client directory to confirm TypeScript compiles without errors
- Run `npm test` in the client directory to confirm existing tests pass
  </action>
  <verify>
Run these commands and confirm they pass:
- `cd /Users/ishiland/Code/nyc-open-routing/client && npm run build` (TypeScript compiles)
- `cd /Users/ishiland/Code/nyc-open-routing/client && npm test -- --run` (tests pass)
- Grep for the key patterns in each file to confirm wiring:
  - `grep 'trafficHour.*trafficDayOfWeek' client/src/hooks/useRouteFetch.ts` (API param wiring)
  - `grep 'trafficHour.*trafficDayOfWeek' client/src/hooks/useIsochroneFetch.ts` (API param wiring)
  - `grep 'params.set.*hour' client/src/hooks/useRouteStateSync.ts` (URL write)
  - `grep 'setTrafficHour' client/src/hooks/useRouteStateSync.ts` (URL restore)
  </verify>
  <done>
All four success criteria confirmed met by existing code. Build succeeds. Tests pass. No code changes required.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create phase SUMMARY documenting pre-existing implementation</name>
  <files>.planning/phases/08-api-and-url-integration/08-01-SUMMARY.md</files>
  <action>
Create the phase SUMMARY documenting that all Phase 8 requirements were already implemented during v2.0 milestone work. The SUMMARY should:

1. State that no code changes were made — this was a verification-only plan
2. Document which files implement each requirement:
   - TIME-04 (API params): useRouteFetch.ts, useIsochroneFetch.ts
   - TIME-05 (URL persistence): useRouteStateSync.ts
3. Document the complete data flow: RoutingContext state -> hooks -> API URLs / URL params -> URL restore -> context state -> DepartureTimePicker render
4. Note that auto-recalculate in ButtonControls watches trafficHour/trafficDayOfWeek
5. Mark all must_haves as verified

Use the SUMMARY template from `@/Users/ishiland/.claude/get-shit-done/templates/summary.md`.
  </action>
  <verify>File `.planning/phases/08-api-and-url-integration/08-01-SUMMARY.md` exists and contains verification results for all four success criteria.</verify>
  <done>Phase SUMMARY created documenting the pre-existing implementation and confirming all requirements are met.</done>
</task>

</tasks>

<verification>
Phase 8 success criteria verification:

1. **Route API calls include hour and day_of_week** — Confirmed in useRouteFetch.ts lines 80-82. Only sent when `mode === "drive" && useTraffic && trafficHour !== null && trafficDayOfWeek !== null`.

2. **Isochrone API calls include hour and day_of_week** — Confirmed in useIsochroneFetch.ts lines 60-63. Same guard condition as route fetch.

3. **Departure time reflected in URL query params** — Confirmed in useRouteStateSync.ts lines 165-168. Writes `hour` and `day` params. Included in effect deps (line 185).

4. **Shared link restores picker to correct day/hour** — Confirmed in useRouteStateSync.ts lines 75-86. Parses `hour`/`day` from URL, validates ranges, calls setTrafficHour/setTrafficDayOfWeek. DepartureTimePicker reads these from context.

All wiring was built during v2.0 milestone. No code changes needed for Phase 8.
</verification>

<success_criteria>
- All four phase success criteria confirmed met by existing code
- TypeScript build succeeds without errors
- Existing tests pass
- Phase SUMMARY created documenting the verification
</success_criteria>

<output>
After completion, create `.planning/phases/08-api-and-url-integration/08-01-SUMMARY.md`
</output>
