---
phase: 05-accessibility-audit
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - client/package.json
  - client/src/__tests__/accessibility/a11y.setup.ts
  - client/src/__tests__/accessibility/aria-labels.test.tsx
  - client/src/__tests__/accessibility/contrast-audit.test.tsx
  - client/src/__tests__/accessibility/keyboard-focus.test.tsx
autonomous: true

must_haves:
  truths:
    - "Automated axe-core audit passes on all key components with zero WCAG 2.1 AA violations"
    - "All interactive elements are findable by their accessible role and name in tests"
    - "Walk mode contrast values in theme are verified as AA compliant via assertion"
    - "BottomSheet keyboard handler tested: ArrowUp/ArrowDown change snap points"
    - "Focus management tested: collapse moves focus to toggle button"
  artifacts:
    - path: "client/src/__tests__/accessibility/a11y.setup.ts"
      provides: "vitest-axe configuration with WCAG 2.1 AA rules and color-contrast disabled for jsdom"
      contains: "configureAxe"
    - path: "client/src/__tests__/accessibility/aria-labels.test.tsx"
      provides: "ARIA label verification tests for TrafficToggle, FerryToggle, InfoModal, ZoomToRouteButton, TimeSelector"
      contains: "toHaveNoViolations"
    - path: "client/src/__tests__/accessibility/contrast-audit.test.tsx"
      provides: "Theme constant assertions for walk mode contrast ratio compliance"
      contains: "MODE_COLORS"
    - path: "client/src/__tests__/accessibility/keyboard-focus.test.tsx"
      provides: "Keyboard navigation tests for BottomSheet and sidebar focus management"
      contains: "ArrowUp"
  key_links:
    - from: "client/src/__tests__/accessibility/a11y.setup.ts"
      to: "vitest-axe"
      via: "npm dependency import"
      pattern: "from.*vitest-axe"
    - from: "client/src/__tests__/accessibility/aria-labels.test.tsx"
      to: "client/src/__tests__/accessibility/a11y.setup.ts"
      via: "axe config import"
      pattern: "from.*a11y\\.setup"
---

<objective>
Install vitest-axe and write automated accessibility tests verifying all WCAG 2.1 Level AA fixes from Plan 01: axe-core audits on key components, ARIA label assertions via getByRole/getByLabelText, theme contrast value verification, keyboard navigation tests for BottomSheet, and focus management tests for sidebar collapse.

Purpose: Provide automated verification that all Phase 5 accessibility fixes are correct and prevent future regressions.
Output: vitest-axe installed, 1 setup file and 3 test files with comprehensive a11y coverage.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-accessibility-audit/05-RESEARCH.md
@.planning/phases/05-accessibility-audit/05-01-SUMMARY.md

Source files under test:
@client/src/components/controls/TravelModeSelect.tsx
@client/src/components/controls/TrafficToggle.tsx
@client/src/components/controls/FerryToggle.tsx
@client/src/components/shared/InfoModal.tsx
@client/src/components/controls/ZoomToRouteButton.tsx
@client/src/components/controls/TimeSelector.tsx
@client/src/components/mobile/BottomSheet.tsx
@client/src/components/layouts/AdaptiveLayout.tsx
@client/src/utils/theme.ts

Existing test infrastructure:
@client/vite.config.ts
@client/src/setupTests.ts
@client/src/testUtils/setup.ts
@client/src/components/controls/Search.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest-axe and create a11y test setup</name>
  <files>
    client/package.json
    client/src/__tests__/accessibility/a11y.setup.ts
  </files>
  <action>
    **Install vitest-axe:**
    Run `cd /Users/ishiland/Code/nyc-open-routing/client && npm install --save-dev vitest-axe` to add vitest-axe (which includes axe-core as a dependency).

    **Create a11y test setup file at `client/src/__tests__/accessibility/a11y.setup.ts`:**
    ```typescript
    import "vitest-axe/extend-expect"
    import { configureAxe } from "vitest-axe"

    // Configure axe for WCAG 2.1 AA rules
    // color-contrast disabled because jsdom does not compute visual styles
    export const a11yAxe = configureAxe({
      rules: {
        "color-contrast": { enabled: false },
      },
      runOnly: {
        type: "tag",
        values: ["wcag2a", "wcag2aa", "wcag21a", "wcag21aa"],
      },
    })
    ```

    This file exports a pre-configured axe instance and registers the `toHaveNoViolations` matcher globally via the `extend-expect` import.
  </action>
  <verify>
    - `cd /Users/ishiland/Code/nyc-open-routing/client && node -e "require('vitest-axe')"` confirms vitest-axe is installed
    - `ls src/__tests__/accessibility/a11y.setup.ts` confirms setup file exists
  </verify>
  <done>
    - vitest-axe is in devDependencies
    - a11y.setup.ts exports configured axe instance with WCAG 2.1 AA rules and color-contrast disabled
  </done>
</task>

<task type="auto">
  <name>Task 2: Write ARIA labels, contrast, and keyboard/focus accessibility tests</name>
  <files>
    client/src/__tests__/accessibility/aria-labels.test.tsx
    client/src/__tests__/accessibility/contrast-audit.test.tsx
    client/src/__tests__/accessibility/keyboard-focus.test.tsx
  </files>
  <action>
    **Create `aria-labels.test.tsx` — A11Y-03 verification:**
    Test that all interactive elements have accessible names. Use the same RoutingContext mock pattern from `Search.test.tsx`. For each component:

    - **TrafficToggle:** Render with `mode: "drive"` context. Assert `screen.getByRole("checkbox", { name: /enable traffic routing/i })` exists. Run `axe(container)` and assert `toHaveNoViolations()`.
    - **FerryToggle:** Render with `mode: "bike"` context. Assert `screen.getByRole("checkbox", { name: /avoid ferries/i })` exists. Run axe and assert no violations.
    - **InfoModal:** Render the component. Assert `screen.getByRole("button", { name: /about nyc open routing/i })` exists. Run axe and assert no violations.
    - **TravelModeSelect:** Render with context. Assert `screen.getByRole("button", { name: /driving directions/i })`, `screen.getByRole("button", { name: /biking directions/i })`, `screen.getByRole("button", { name: /walking directions/i })` all exist. Run axe and assert no violations.
    - **TimeSelector:** Render with `mode: "drive"` and `useTraffic: true` context. Assert `screen.getByRole("slider", { name: /hour of day/i })` exists. Run axe and assert no violations.

    Note: ZoomToRouteButton requires map + route context which is complex to mock; skip axe test for it but verify aria-label is present in source via a simple render test or grep in Plan 01's verify step.

    Import `a11yAxe` from `./a11y.setup` and use it in place of the default `axe` import for each test. Import `axe` from `vitest-axe` for the actual audit calls. The setup file's `extend-expect` import registers the matcher.

    Use the same `mockContextValue` pattern from `Search.test.tsx` to create a shared `RoutingContext.Provider` wrapper. Create a local `renderWithContext` helper in each test file (or a shared one).

    **Create `contrast-audit.test.tsx` — A11Y-01 verification:**
    Since jsdom cannot compute visual contrast, verify the theme constants are correct:

    ```typescript
    import { MODE_COLORS } from "../../utils/theme"

    // Relative luminance calculation (WCAG formula)
    function relativeLuminance(hex: string): number {
      const r = parseInt(hex.slice(1, 3), 16) / 255
      const g = parseInt(hex.slice(3, 5), 16) / 255
      const b = parseInt(hex.slice(5, 7), 16) / 255
      const toLinear = (c: number) => (c <= 0.03928 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4))
      return 0.2126 * toLinear(r) + 0.7152 * toLinear(g) + 0.0722 * toLinear(b)
    }

    function contrastRatio(hex1: string, hex2: string): number {
      const l1 = relativeLuminance(hex1)
      const l2 = relativeLuminance(hex2)
      const lighter = Math.max(l1, l2)
      const darker = Math.min(l1, l2)
      return (lighter + 0.05) / (darker + 0.05)
    }
    ```

    Tests:
    - "walk mode selected state meets AA contrast (4.5:1)" — assert `contrastRatio(MODE_COLORS.walk, "#000000") >= 4.5` (black text on walk orange)
    - "drive mode selected state meets AA contrast (4.5:1)" — assert `contrastRatio(MODE_COLORS.drive, "#ffffff") >= 4.5` (white text on MTA blue)
    - "bike mode selected state meets AA contrast (4.5:1)" — assert `contrastRatio(MODE_COLORS.bike, "#ffffff") >= 4.5` (white text on dark green)

    **Create `keyboard-focus.test.tsx` — A11Y-02 and A11Y-04 verification:**
    - **BottomSheet keyboard navigation:** Render BottomSheet. Get the drag handle via `screen.getByRole("slider", { name: /resize bottom sheet/i })`. Use `userEvent.setup()` to:
      - Focus the slider, press ArrowUp, verify `aria-valuenow` increases
      - Press ArrowDown, verify `aria-valuenow` decreases
      - This confirms the keyboard handler is wired and functional
    - **Sidebar focus management:** Render AdaptiveLayout with sidebar content. Get the collapse button via `screen.getByRole("button", { name: /collapse sidebar/i })`. Click it. After a short wait (use `vi.useFakeTimers` + `vi.advanceTimersByTime(260)`), verify the expand button has focus. This tests that focus moves to the toggle button after collapse.

    For BottomSheet: it uses SwipeableDrawer internally which needs MUI ThemeProvider. Wrap renders in `<ThemeProvider theme={theme}>`. Mock `window.innerHeight` as needed.

    For AdaptiveLayout: it reads from MapInstanceContext, so provide a mock. The sidebar content can be a simple `<div>` with an input.
  </action>
  <verify>
    Run `cd /Users/ishiland/Code/nyc-open-routing/client && npm test` — all tests pass including the new a11y tests.
    Confirm test files exist:
    - `ls src/__tests__/accessibility/aria-labels.test.tsx`
    - `ls src/__tests__/accessibility/contrast-audit.test.tsx`
    - `ls src/__tests__/accessibility/keyboard-focus.test.tsx`
  </verify>
  <done>
    - aria-labels.test.tsx verifies all interactive elements have accessible names and pass axe-core audit
    - contrast-audit.test.tsx verifies MODE_COLORS meet WCAG AA 4.5:1 contrast ratios with their text colors
    - keyboard-focus.test.tsx verifies BottomSheet keyboard navigation and sidebar focus management
    - All tests pass with `npm test`
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/ishiland/Code/nyc-open-routing/client && npm test` — all tests pass (existing + new a11y tests)
2. `cd /Users/ishiland/Code/nyc-open-routing/client && npx tsc --noEmit` — zero type errors
3. New test files cover: axe-core audits (A11Y-03), contrast verification (A11Y-01), keyboard navigation (A11Y-02), focus management (A11Y-04)
4. vitest-axe in package.json devDependencies
</verification>

<success_criteria>
- vitest-axe installed and configured for WCAG 2.1 AA
- 3 test files written and passing
- axe-core audits pass on TrafficToggle, FerryToggle, InfoModal, TravelModeSelect, TimeSelector
- Contrast ratio assertions verify walk (#E65100 + black >= 4.5:1), drive (#0039A6 + white >= 4.5:1), bike (#087F23 + white >= 4.5:1)
- Keyboard tests verify BottomSheet drag handle ArrowUp/ArrowDown
- Focus management tests verify sidebar collapse moves focus to toggle button
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-accessibility-audit/05-02-SUMMARY.md`
</output>
